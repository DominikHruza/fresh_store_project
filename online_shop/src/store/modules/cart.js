const state = {
  itemsInCart: [],
  cartTotal: [],
};

const mutations = {
  SET_ITEM_TO_CART() {},

  REMOVE_FROM_CART() {},
};

const actions = {
  addCartItem() {
    const item = {};
  },

  removeCartItem() {},

  incrementItem() {},

  decrementItem() {},
};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
